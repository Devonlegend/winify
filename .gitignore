# Local config (may contain secrets) and runtime data
/config.yaml
/data/

# Build artifacts
/control-center.exe
/control-center
*.test
CLAUDE.md

# Local test harness runtime state (keys, database, binary)
/dev/data/
/dev/keys/
/dev/control-center.exe

# Public-domain tool binaries uploaded to targets (nssm.exe, caddy.exe)
/tools/

# Installer build output
/packaging/payload/
/packaging/dist/