# Deployment targets. Credentials are referenced by name; secret values live
# encrypted in SQLite (add with: control-center cred add <name>).
#
# This file seeds the database only on a fresh install (empty tables). After
# that the database is authoritative and the dashboard manages the inventory,
# so add servers in the UI rather than here.
servers: []
